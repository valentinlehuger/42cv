cmake_minimum_required(VERSION 2.8)
project( createFaceDB )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( detect detect_face.cpp )
target_link_libraries( detect ${OpenCV_LIBS} )